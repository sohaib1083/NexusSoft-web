const nextConfig = {
  experimental: {
    appDir: true, // If using the App Router
    eslint: {
      ignoreDuringBuilds: true,
      eslint: {
        ignoreDuringBuilds: true,
      },
    },
  },
};

export default nextConfig;
